<%- include("../partials/headera") %>
<div class="col-lg-6 col-md-12" id="form">
    <div class="row justify-content-center">
        <div class="col-lg-8 col-md-12" style="margin-top: 10%">
            <div id="form1">
                <a href="/dashboard">
                    <i class="fas fa-arrow-left"></i>
                    Wróć do panelu administracyjnego
                </a>
                <h1 class="login">Dodaj odpowiedź na pytanie <q><%= question.title %></q></h1>
                <% if(error.length > 0){ %>
                    <div class="alert alert-danger" role="alert">
                        <%= error %>
                    </div>
                <% } %>
                <% if(success.length > 0){ %>
                    <div class="alert alert-success" role="alert">
                        <%= success %>
                    </div>
                <% } %>
                <form action="/questions/<%= question._id %>/answers" method="post">
                   
                    <label for="username" class="username">
                        Wybierz typ
                        
                    </label>
                    <select name="type" class="form-control">
                        <option>Poprawna</option>
                        <option>Niepoprawna</option>
                    </select>
                    <br>
                    
                    <label for="username" class="username">
                       Podaj tekst odpowiedzi
                        
                    </label>
                    <textarea name="text" placeholder="Mulan" class="form-control"></textarea>
                    <br>
                    
                    <input type="submit" value="Dodaj odpowiedź na pytanie" class="btn btn-danger button">
                   
                </form>
            </div>
           
        </div>
        
    </div>
</div>

</body>
</html>